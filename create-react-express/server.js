const express = require("express");
const path = require("path");
const PORT = process.env.PORT || 3001;
const app = express();

// This is where I would use the express app server and code for serving to heroku and also set the api routes

