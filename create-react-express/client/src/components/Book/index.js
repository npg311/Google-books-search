import React from "react";
import { Row, Col } from "../Grid";
import { ListItem } from "../List";
import "./style.css";

// this is where I would put the book function to add the button for submitting a new book and the
// info that goes along with it ie function Book({ title, subtitle, authors, link, description, image, Button })
// and then finally export the book function at the end of the code 


